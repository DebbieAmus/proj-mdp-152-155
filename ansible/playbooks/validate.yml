---
- name: Validate KOPS Cluster
  hosts: k8
  become: yes
  tasks:
    - name: Validate KOPS cluster
      shell: kops validate cluster --state=s3://k8sbucket111
      register: validation_output
 
    - name: Show cluster validation result
      debug:
        var: validation_output.stdout
...
